<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tv_item_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="address"
        android:textSize="25sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tv_item_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="100dp"
        app:layout_constraintStart_toEndOf="@id/tv_item_address"
        app:layout_constraintTop_toTopOf="@id/tv_item_address"
        app:layout_constraintBottom_toBottomOf="@id/tv_item_address"
        android:text="score"
        android:textSize="25sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/iv_item_line"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_item_address"
        android:background="@color/gray" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/iv_item_favorite"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="40dp"
        app:layout_constraintStart_toEndOf="@id/tv_item_score"
        app:layout_constraintTop_toTopOf="@id/tv_item_score"
        app:layout_constraintBottom_toBottomOf="@id/tv_item_score"
        android:background="@drawable/ic_recommend_black_24dp" />

</androidx.constraintlayout.widget.ConstraintLayout>